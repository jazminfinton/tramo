# Specs

Source-of-truth specs, organized by domain: `{domain}/spec.md`.

Empty on this greenfield project. `sdd-archive` populates it by merging delta specs from `openspec/changes/{change-name}/specs/` into the matching domain file as each change is archived.
